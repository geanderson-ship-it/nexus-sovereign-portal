version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use 20 || nvm install 20 || true
        - node -v
        - npm install --legacy-peer-deps
        - env | grep -e GMAIL_ -e EVOLUTION_ -e BEDROCK_ -e NEXT_PUBLIC_ -e NEXUS_ -e ZAPI_ -e ELEVENLABS_ -e AZURE_ > .env.production || true
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - .next/cache/**/*
      - node_modules/**/*
